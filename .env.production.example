# EJEMPLO para producción.
# NO renombres este archivo a .env.production dentro del repositorio.
# En el servidor/VPS crea .env.production y coloca allí los valores reales.
NODE_ENV=production
PORT=3000

DB_HOST=localhost
DB_USER=usuario_produccion
DB_PASSWORD=CAMBIAR
DB_NAME=futbol_equipos

JWT_SECRET=CAMBIAR_POR_UN_SECRETO_LARGO_Y_ALEATORIO
JWT_EXPIRES_IN=1h
